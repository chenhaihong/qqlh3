// import { App } from "vue";

// const install = (app: App) => {
//   app.mixin({
//     custom: "Mixin message!",
//   });
//   app.config.optionMergeStrategies.custom = (parentVal, childVal) => {
//     return childVal || parentVal; // 优先使用子实例的值
//   };
// };

// export { install };
